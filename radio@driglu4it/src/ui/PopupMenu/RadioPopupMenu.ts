import { createPopupMenu } from "cinnamonpopup";

type PopupMenuProps = Parameters<typeof createPopupMenu>[0]

type Props = PopupMenuProps & {

}

export function createRadioPopupMenu(props: PopupMenuProps) {
    const { launcher } = props

    // const channelList = 

}